require 'spec_helper'

module SteppingStone
  describe RbServer do
    describe "#start_test" do
      it "starts a new session"
      it "yields the new session to the given block"
      it "stops the test when called with a block"
    end
  end
end
